{
  "name": "backoffice",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": "20.x",
    "pnpm": "9.15.1"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
